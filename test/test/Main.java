package test;

public class Main {
    public static void main(String[] args){
        //TODO: Implement the MainMenu GUI.

        /*
        (?)Do we need mainMenu class, can be implemented here.
                Menu mainMenu = new MainMenu();
                mainMenu.show();
        PRESS -1 (Quit):
                end.
        PRESS 1 (Create a Recipe):
                Prompt for String RecipeName
                While(not done):
                    ask for ingredients (stored as ArrayList);
                       *need 3 parameters --> name, amount, metric (ArrayList of strings)
                    give option of add or done (?)
                While (not done):
                    ask for directions (stored as LinkedList)
                    give option of add or done (?)
                (need to instantiate): new Recipe(STRING recipename, ARRAYLIST ingredients, LIST directions)
       PRESS 2 (Retrieve a Recipe):
                Prompt for String RecipeName
                RetrieveRecipeMenu(STRING recipename)
       PRESS 3 (Explore all the Recipes):
                    //Wait to see how this is implemented to determine parameter
                ExploreRecipeMenu()

         */
    }
}
